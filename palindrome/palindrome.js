// Solution 1
function palindrome(str) {
    // Store all array methods into reversed variable first
    // We split first
    // Reverse the string
    // Join 
    // Return reversed
    const reversed = str.split('').reverse().join();
    return str = reversed;
}

module.exports = palindrome;