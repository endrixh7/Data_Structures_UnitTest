const fizzbuzz = require('./fizzbuzz');

describe('fizzbuzz', ()=>{
    it('should be a function', ()=>{
        expect(typeof fizzbuzz).toEqual('function');
    });

    // Create test that check if is divisible by 3
    // return

    // Create test that check if is divisible by 5
    //return

    // Create test that check if is divisible by 3 and 5
    // return
    // Hint: % 15 === 0 Return FizzBuzz
});


